# Visión Artificial (URJC) - Prácticas

## Setup (Windows + Git Bash)

### Opción A: Script (recomendado)

```bash
bash scripts/setup.sh
source .venv/Scripts/activate
```

📌 Nota: `setup.sh` **no puede “dejar activado”** el venv en tu terminal actual (por cómo funcionan los scripts).  
Por eso después pones tú `source .venv/Scripts/activate`.
